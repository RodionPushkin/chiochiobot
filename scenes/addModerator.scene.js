const {Scenes,Markup} = require('telegraf')
module.exports = new Scenes.WizardScene(
    'addModerator',
    async (ctx) => {
        try {
        } catch (err){
            console.log(err)
        }
    },
)